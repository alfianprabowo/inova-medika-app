import { StyleSheet, View } from "react-native";

export default function ReservationHistoryPage(params) {
    return (
        <View>

        </View>
    )
};

const styles = StyleSheet.create({

});
