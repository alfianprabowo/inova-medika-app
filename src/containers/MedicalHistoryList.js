import React from "react";
import { StyleSheet, View } from "react-native";

export default class MedicalHistoryList extends React.PureComponent {
    render() {
        return (
            <View>

            </View>
        )
    }
};

const styles = StyleSheet.create({

});
